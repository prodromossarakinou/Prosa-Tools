import { ToolsOverview } from "@/features/tools/components/tools-overview";

export default function DashboardPage() {
  return (
    <ToolsOverview
      heading="Dashboard"
      description="Track all current and upcoming tools from one place as you build through the 100 Days with AI challenge."
    />
  );
}
